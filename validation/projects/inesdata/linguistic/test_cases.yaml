suite: linguistic
project: inesdata
profile: read-only
cases:
  - id: INESDATA-LING-01
    title: Linguistic portal access
    type: playwright
    profile: read-only
    status: proposed
    coverage_status: scaffold_external_target
    mapping_status: external_project_scaffold
    execution_mode: read_only_target_scaffold
    objective: Verify that the linguistic dataspace portal is available to the validation user.
    preconditions:
      - External target configured with `dataspaces[].name=linguistic`.
      - Validation credentials resolved from the environment, secret manager or secure prompt.
    evidence:
      - Playwright screenshot or trace.
      - Validated URL redacted when required by the target.
    automation:
      status: proposed
      mode: read_only_playwright
      enabled_by_default: false
      notes: Scaffold for external INESData targets. It is not part of Level 6 A5.2 default coverage until a target explicitly enables it.
  - id: INESDATA-LING-02
    title: Linguistic catalog is visible
    type: playwright
    profile: read-only
    status: proposed
    coverage_status: scaffold_external_target
    mapping_status: external_project_scaffold
    execution_mode: read_only_target_scaffold
    objective: Verify that the user can open the linguistic catalog and see published results.
    preconditions:
      - The target defines `public_portal_url` for the linguistic dataspace.
      - Public or approved data exists for read-only validation.
    evidence:
      - Catalog screenshot.
      - Non-sensitive visible count or text.
    automation:
      status: proposed
      mode: read_only_playwright
      enabled_by_default: false
      notes: Scaffold for external INESData targets. It is not part of Level 6 A5.2 default coverage until a target explicitly enables it.
  - id: INESDATA-LING-03
    title: Linguistic dataspace API smoke
    type: newman
    profile: read-only
    status: proposed
    coverage_status: scaffold_external_target
    mapping_status: external_project_scaffold
    execution_mode: read_only_target_scaffold
    objective: Reuse Newman core against linguistic connectors when the target provides compatible endpoints.
    preconditions:
      - Provider/consumer connectors are defined in the target.
      - Management API is accessible to the validation user.
    evidence:
      - Newman JSON report.
    automation:
      status: proposed
      mode: read_only_newman
      enabled_by_default: false
      notes: Scaffold for external INESData targets. It is not part of Level 6 A5.2 default coverage until a target explicitly enables it.
